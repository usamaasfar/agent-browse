// xmcp auto-discovers tools from src/tools/ and registers them as MCP tools.
// Run `bun run dev` to start the development server (HTTP on :3001/mcp + STDIO).
// Run `bun run build` to build for production, then `node dist/http.js` or `node dist/stdio.js`.
